/// <reference types="astro/client" />

/**
 * Injected at build time from package.json by astro.config.mjs.
 * Read by the About panel (docs/03 §6) and the diagnostics payload (docs/02 §7).
 */
declare const __TT_VERSION__: string;
