---
// Required: wrangler.jsonc sets not_found_handling "404-page", which serves
// dist/404.html for unknown paths. That is the whole 404 story (docs/10 §3, §8)
// — no Cloudflare zone feature involved. This VI page is that file; `/en/404`
// is a reachable mirror but is never the fallback (see TtNotFound.astro).
import TtNotFound from '../components/landing/TtNotFound.astro';
import { vi } from '../i18n/static/vi';
---

<TtNotFound t={vi} lang="vi" />
