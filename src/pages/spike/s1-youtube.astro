---
import TtBase from '../../layouts/TtBase.astro';
---

<TtBase title="spike/s1-youtube — TickTune" noindex>
  <!--
    THROWAWAY. docs/15 §Scope rule calls S1/S3/S4 throwaway harnesses; these live
    in main rather than on a branch so they can be run against the deployed site,
    which is where the interesting browser behaviour actually happens — and for
    S1 that matters more than for the others, because the shipped CSP is part of
    what is being measured (docs/09 §4 allows www.youtube.com but not s.ytimg.com).
    Delete all /spike routes before P6 (docs/16) — tracked in docs/15 §Exit.
  -->
  <div id="tt-spike"></div>
  <script>
    if (!document.documentElement.dataset.ttBlocked) {
      import('../../app/spike/mount-s1.ts');
    }
  </script>
</TtBase>
